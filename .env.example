# Portfolio assistant (api/chat.ts) — set this in the Vercel dashboard, never commit a real key.
# Without it, the chatbot returns a graceful "reach out" fallback and the site still builds.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
